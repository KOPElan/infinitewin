<Window x:Class="InfiniteWin.WindowSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Window" 
        Height="500" Width="600"
        Background="#FF2B2B2B"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Select a window to add to the canvas:" 
                   Foreground="White" 
                   FontSize="14" 
                   Margin="0,0,0,10"/>

        <!-- Window List -->
        <Border Grid.Row="1" 
                BorderBrush="#FF6496FF" 
                BorderThickness="1" 
                CornerRadius="4">
            <ListBox x:Name="WindowListBox"
                     Background="#FF1E1E1E"
                     Foreground="White"
                     BorderThickness="0"
                     MouseDoubleClick="WindowListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF6496FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF4A76DD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button x:Name="OkButton" 
                    Content="OK" 
                    Width="80" 
                    Padding="5" 
                    Margin="5,0"
                    Click="OkButton_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Padding="5" 
                    Margin="5,0"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
